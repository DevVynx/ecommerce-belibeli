import { google } from "./google";
import { login } from "./login";
import { refresh } from "./refresh";
import { register } from "./register";

const validations = { google, login, register, refresh };

export default validations;
